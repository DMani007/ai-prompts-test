Transaction under review:
- transaction_id:  {{ transaction_id }}
- amount_usd:      {{ amount_usd }}
- merchant_name:   {{ merchant_name }}
- merchant_mcc:    {{ merchant_mcc }}
- country:         {{ country }}
- device_id:       {{ device_id }}
- device_is_new:   {{ device_is_new }}

Cardholder baseline (last 90 days):
- p95_amount_usd:    {{ p95_amount_usd }}
- common_mccs:       {{ common_mccs | join(", ") }}
- common_countries:  {{ common_countries | join(", ") }}

Classify per the system instructions. Reply with the JSON object only.
